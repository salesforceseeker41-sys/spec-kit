id: TEST-004
title: Integration Test Evidence
category: Testing
description: Integration features must define test evidence for contracts, retries, timeouts, and failures.
rationale: Integration failures are common enterprise risk points and need explicit test evidence.
severity: advisory
default_enabled: true
applies_to:
  - specification
  - plan
  - tasks
  - testing
keywords:
  - integration test
  - contract
  - retry
  - timeout
recommendation: Identify mock, sandbox, contract, and end-to-end evidence needed for integration confidence.
references:
  - Enterprise Integration Testing Standards
owner: Platform Team
version: "1.0"
